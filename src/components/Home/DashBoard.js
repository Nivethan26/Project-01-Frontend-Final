import React from 'react';

const Dashboard = () => {
    const headingStyle = {
        textAlign: 'center',  // Use textAlign for centering text
    };

    return (
        <div>
            <h2 style={headingStyle}>Welcome</h2>
        </div>
    );
};

export default Dashboard;
